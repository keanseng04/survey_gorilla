<% if session[:user_id] == @user.id %>


    <form action="/user/<%= @user.id %>/album/<%= @album.id %>" method="post" enctype="multipart/form-data"></div>
      <input class="uploads" type="file" name="image" />
      <input type="submit" name="submit" value="Upload" />
    </form>
<div id="break"></div>

<% end %>

    <% @album.uploads.each do |upload| %>
      <img src="<%= upload.file %>" />
    <% end %>

  <div class="links">
    <a href="/">Home</a>
  </div>